# sampleSrc 메타데이터 분석 정답지 (수정판)

## 개요

본 보고서는 **sampleSrc 프로젝트의 소스 코드를 직접 분석**하여 메타데이터베이스 생성 시 **이론적으로 생성되어야 할 정답**을 정리한 것입니다. 이 정답지와 실제 개발된 메타디비 시스템의 결과를 비교하여 시스템의 정확성을 검증합니다.

**분석 일시**: 2025-09-19 (수정)  
**분석 대상**: D:\Analyzer\CreateMetaDb\projects\sampleSrc (소스 파일 직접 분석)  
**목적**: 메타디비 시스템 정확성 검증을 위한 **정답지** 작성  
**분석 방법**: 소스 코드 직접 파싱 및 분석  

## 1. 프로젝트 구조 분석

### 1.1 소스 파일 분석 결과 (정답지)
- **프로젝트명**: sampleSrc
- **프로젝트 경로**: D:\Analyzer\CreateMetaDb\projects\sampleSrc
- **프로젝트 해시값**: `-` (하드코딩된 상수값)
- **전체 파일 수**: 123개
  - **Java 파일**: 55개 (컨트롤러, 서비스, DAO, 모델 등)
  - **XML 파일**: 16개 (MyBatis Mapper)
  - **JSP 파일**: 12개 (프론트엔드 페이지)
  - **JSX 파일**: 5개 (React 컴포넌트)
  - **CSV 파일**: 3개 (스키마 정의)
  - **기타 파일**: 32개 (설정, 정적 리소스 등)

### 1.2 주요 디렉토리 구조
```
sampleSrc/
├── src/main/java/com/example/
│   ├── controller/       # 9개 파일 (컨트롤러 레이어)
│   ├── service/          # 10개 파일 (서비스 레이어)
│   ├── dao/              # 7개 파일 (데이터 액세스 레이어)
│   ├── model/            # 2개 파일 (모델 레이어)
│   ├── mapper/           # 2개 파일 (매퍼 인터페이스)
│   └── 기타/              # 25개 파일 (유틸, 에러 처리 등)
├── src/main/resources/mybatis/mapper/  # 16개 XML 파일
├── src/main/webapp/      # 12개 JSP 파일 + 5개 JSX 파일
└── db_schema/            # 4개 CSV/SQL 파일
```

## 2. 데이터베이스 스키마 정보 (2단계)

### 2.1 테이블 정보 분석

#### 2.1.1 실제 스키마 테이블 (17개 - CSV 기준)
| OWNER | TABLE_NAME | COMMENTS | ERD 표시 여부 |
|-------|------------|----------|---------------|
| SAMPLE | USERS | 사용자 정보를 관리하는 테이블 | ✅ (관계 있음) |
| SAMPLE | USER_TYPES | 사용자 타입을 정의하는 테이블 | ✅ (관계 있음) |
| SAMPLE | CUSTOMERS | 고객정보 | ✅ (관계 있음) |
| SAMPLE | ORDERS | 주문 | ✅ (관계 있음) |
| SAMPLE | ORDER_ITEMS | 주문상품 | ✅ (관계 있음) |
| SAMPLE | PRODUCTS | 상품 | ✅ (관계 있음) |
| SAMPLE | CATEGORIES | 분류코드 | ✅ (관계 있음) |
| SAMPLE | BRANDS | 브랜드 | ✅ (관계 있음) |
| SAMPLE | SUPPLIERS | 공급업체 | ❌ (고아 테이블) |
| SAMPLE | WAREHOUSES | 창고 | ❌ (고아 테이블) |
| SAMPLE | INVENTORIES | 재고 | ❌ (고아 테이블) |
| SAMPLE | PRODUCT_REVIEWS | 상품리뷰 | ❌ (고아 테이블) |
| SAMPLE | DISCOUNTS | 할인 | ❌ (고아 테이블) |
| PUBLIC | USER_ROLE | 사용자역할 | ❌ (고아 테이블 - 테이블명 불일치) |
| SCOTT | DYNAMIC_DATA | 동적데이터 | ✅ (관계 있음) |
| SCOTT | RELATED_DATA | 관련데이터 | ✅ (관계 있음) |

#### 2.1.2 INFERRED 테이블 (SQL에서 추론된 테이블)
| OWNER | TABLE_NAME | 추론 소스 | ERD 표시 여부 |
|-------|------------|----------|---------------|
| UNKNOWN | USER_ROLES | UserMapper.xml JOIN 쿼리 | ✅ (USERS와 관계) |
| UNKNOWN | ROLES | UserMapper.xml JOIN 쿼리 | ✅ (USER_ROLES와 관계) |
| UNKNOWN | USER_PROFILES | DirectXmlQueryMapper.xml | ✅ (USERS와 관계) |
| UNKNOWN | PAYMENTS | DirectXmlQueryMapper.xml | ✅ (ORDERS와 관계) |
| UNKNOWN | DEPARTMENTS | UserMapper.xml | ✅ (USERS와 관계) |

### 2.2 컬럼 정보 (ALL_TAB_COLUMNS.csv)
총 컬럼 수: **92개**

#### 주요 테이블별 컬럼 구성:
**USERS 테이블 (13개 컬럼)**:
- ID (NUMBER, PK): 사용자 고유 식별자
- USERNAME (VARCHAR2(50)): 사용자명 (유니크)
- EMAIL (VARCHAR2(100)): 이메일 주소
- PASSWORD (VARCHAR2(100)): 암호화된 비밀번호
- NAME (VARCHAR2(100)): 사용자 실명
- AGE (NUMBER(3)): 나이
- STATUS (VARCHAR2(20)): 계정 상태 (ACTIVE/INACTIVE)
- USER_TYPE (VARCHAR2(20)): 사용자 타입 (NORMAL/PREMIUM/ADMIN)
- PHONE (VARCHAR2(20)): 전화번호
- ADDRESS (VARCHAR2(200)): 주소
- CREATED_DATE (DATE): 계정 생성일
- UPDATED_DATE (DATE): 최종 수정일

**PRODUCTS 테이블 (12개 컬럼)**:
- PRODUCT_ID (VARCHAR2, PK): 상품ID
- PRODUCT_NAME (VARCHAR2): 상품명
- DESCRIPTION (CLOB): 상품설명
- PRICE (NUMBER): 가격
- STOCK_QUANTITY (NUMBER): 재고수량
- STATUS (VARCHAR2): 상태
- CATEGORY_ID (VARCHAR2): 분류ID
- BRAND_ID (VARCHAR2): 브랜드ID
- SUPPLIER_ID (VARCHAR2): 공급업체ID
- WAREHOUSE_ID (VARCHAR2): 창고ID
- CREATED_DATE (DATE): 생성일
- UPDATED_DATE (DATE): 수정일
- DEL_YN (CHAR): 삭제여부

### 2.3 ERD 관계 분석 (정답지)

#### 2.3.1 표시되어야 할 관계들 (INFERRED 테이블 기반)
**핵심 관계 체인**:
```
USERS ←→ USER_ROLES ←→ ROLES     # 사용자 권한 관리 (INFERRED)
USERS ←→ USER_PROFILES           # 사용자 프로필 (INFERRED)
USERS ←→ DEPARTMENTS             # 사용자 부서 (INFERRED)
ORDERS ←→ PAYMENTS               # 주문 결제 (INFERRED)
CUSTOMERS ←→ ORDERS ←→ ORDER_ITEMS ←→ PRODUCTS  # 주문 플로우 (실제)
PRODUCTS ←→ CATEGORIES           # 상품 분류 (실제)
PRODUCTS ←→ BRANDS               # 상품 브랜드 (실제)
DYNAMIC_DATA ←→ RELATED_DATA     # 동적 데이터 (실제)
```

#### 2.3.2 ERD에 표시되지 않는 고아 테이블들
- `PUBLIC.USER_ROLE` - 테이블명 불일치로 고립 (SQL은 USER_ROLES 사용)
- `SAMPLE.SUPPLIERS` - JOIN 쿼리에서 사용되지 않음
- `SAMPLE.WAREHOUSES` - JOIN 쿼리에서 사용되지 않음  
- `SAMPLE.INVENTORIES` - JOIN 쿼리에서 사용되지 않음
- `SAMPLE.PRODUCT_REVIEWS` - JOIN 쿼리에서 사용되지 않음
- `SAMPLE.DISCOUNTS` - JOIN 쿼리에서 사용되지 않음

#### 2.3.3 시스템 동작 원리 (정답)
1. **실제 스키마 테이블**: CSV에서 로드 → 메타디비에 저장
2. **INFERRED 테이블**: SQL JOIN 분석 → 메타디비에 추론 테이블로 저장
3. **ERD 생성**: 관계가 있는 테이블만 표시 (고아 테이블 자동 제외)
4. **결과**: 의미있는 관계 중심의 깔끔한 ERD 생성

### 2.4 소스 분석 기반 컴포넌트 정답지

#### 2.4.1 Java 컴포넌트 (소스 분석 결과)
- **CLASS 컴포넌트**: 51개 (실제 Java 클래스)
  - 주요 클래스: CoreSqlPatternDao, MicroserviceController, UserController
  - 서비스 클래스: BaseService, ConcreteService, MicroserviceService
  - 서블릿 클래스: AdvancedReportServlet, DirectQueryServlet 등
- **METHOD 컴포넌트**: 340개 (실제 Java 메서드)
  - 컨트롤러 메서드, 서비스 메서드, DAO 메서드 등
  - 어노테이션 기반 메서드들 포함

#### 2.4.2 SQL 관련 컴포넌트 (소스 분석 결과)
- **SQL 쿼리**: 193개 (XML + Java에서 추출)
  - SELECT, INSERT, UPDATE, DELETE 쿼리들
  - MyBatis XML 매퍼의 모든 쿼리
  - Java 코드 내 문자열 SQL
- **SQL에서 추출된 테이블**: 64개
  - 실제 테이블: USERS, ORDERS, PRODUCTS, CATEGORIES, BRANDS 등
  - 추론 테이블: DEPARTMENTS, USER_ROLES, ROLES, PAYMENTS 등
  - 환경별 테이블: USERS_, ORDERS_, PRODUCTS_ 등
  - 테스트 테이블: NONEXISTENT_TABLE 등

#### 2.4.3 API 및 프론트엔드 컴포넌트 (소스 분석 결과)
- **API 엔드포인트**: 43개 (실제 어노테이션에서 추출)
  - REST API: /api, /users, /products, /orders 등
  - 컨트롤러 매핑: /user-management, /dashboard 등
- **프론트엔드 페이지**: 17개
  - JSP 페이지: 12개 (user/list.jsp, product/list.jsp 등)
  - JSX 컴포넌트: 5개 (React 컴포넌트들)

#### 2.4.4 테이블 및 컬럼 컴포넌트 (정답지)
- **실제 스키마 테이블**: 17개 (CSV 기준)
- **SQL에서 추론되는 테이블**: 47개 추가
  - 환경별 테이블 (USERS_, ORDERS_ 등)
  - 비즈니스 로직 테이블 (DEPARTMENTS, USER_ROLES 등)
  - 분석용 테이블 (CUSTOMER_BEHAVIOR_METRICS 등)
- **총 테이블**: 64개
- **컬럼**: 실제 92개 + 추론 컬럼들

#### 2.4.5 전체 컴포넌트 정답지 요약
- **Java 클래스**: 51개
- **Java 메서드**: 340개  
- **SQL 쿼리**: 193개
- **테이블**: 64개 (17개 실제 + 47개 추론)
- **API 엔드포인트**: 43개
- **프론트엔드 페이지**: 17개
- **예상 총 컴포넌트**: 약 700-800개

## 3. XML 분석 정보 (3단계)

### 3.1 분석 대상 XML 파일
총 **16개** MyBatis Mapper XML 파일

#### 주요 XML 파일:
1. **UserMapper.xml**: 사용자 관련 쿼리 (17개 쿼리)
2. **ProductMapper.xml**: 상품 관련 쿼리 (9개 쿼리)
3. **ComplexEnterpriseMapper.xml**: 복잡한 기업용 쿼리
4. **MicroserviceMapper.xml**: 마이크로서비스 쿼리
5. **기타 매퍼들**: 테스트용 및 특수 목적 쿼리

### 3.2 UserMapper.xml 상세 분석

#### 쿼리 구성 (17개):
1. **findUsersByCondition** (SQL_SELECT)
   - 테이블: USERS
   - 조건: 동적 검색 (username, email, name, phone)
   - 페이징: LIMIT/OFFSET 지원

2. **findUsersByAdvancedCondition** (SQL_SELECT)
   - 테이블: USERS
   - 조건: 고급 검색 (나이 범위, 상태 목록, 정렬)
   - 복잡한 동적 SQL

3. **findUsersByType** (SQL_SELECT)
   - 테이블: USERS
   - 조건: 사용자 타입별 조회

4. **findUserById** (SQL_SELECT)
   - 테이블: USERS
   - 조건: 단일 사용자 조회

5. **createUser** (SQL_INSERT)
   - 테이블: USERS
   - 기능: 새 사용자 생성

6. **updateUser** (SQL_UPDATE)
   - 테이블: USERS
   - 기능: 사용자 정보 수정

7. **deleteUser** (SQL_UPDATE)
   - 테이블: USERS
   - 기능: 논리적 삭제 (del_yn = 'Y')

8. **existsByUsername** (SQL_SELECT)
   - 테이블: USERS
   - 기능: 사용자명 중복 확인

9. **existsByEmail** (SQL_SELECT)
   - 테이블: USERS
   - 기능: 이메일 중복 확인

10. **isEmailVerified** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 이메일 인증 여부 확인

11. **isPhoneVerified** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 전화번호 인증 여부 확인

12. **getTotalUserCount** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 전체 사용자 수 조회

13. **getActiveUserCount** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 활성 사용자 수 조회

14. **getNewUsersTodayCount** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 오늘 가입 사용자 수

15. **getPremiumUserCount** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 프리미엄 사용자 수

16. **updateLastLogin** (SQL_UPDATE)
    - 테이블: USERS
    - 기능: 마지막 로그인 시간 업데이트

17. **getUserStatisticsByStatus** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 상태별 사용자 통계

18. **getUserStatisticsByType** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 타입별 사용자 통계

19. **getMonthlyRegistrationStats** (SQL_SELECT)
    - 테이블: USERS
    - 기능: 월별 가입자 통계

20. **findUsersWithOrderInfo** (SQL_SELECT)
    - 테이블: USERS, ORDERS (LEFT JOIN)
    - 기능: 사용자와 주문 정보 조회
    - JOIN 관계: USERS.user_id = ORDERS.user_id

21. **findUsersByComplexSearch** (SQL_SELECT)
    - 테이블: USERS, ORDERS (LEFT JOIN)
    - 기능: 복잡한 동적 검색
    - JOIN 관계: USERS.user_id = ORDERS.user_id

### 3.3 ProductMapper.xml 상세 분석

#### 쿼리 구성 (9개):
1. **selectProductById** (SQL_SELECT)
   - 테이블: PRODUCTS

2. **selectProductsByCondition** (SQL_SELECT)
   - 테이블: PRODUCTS
   - 동적 조건: name, category, status, delYn

3. **selectProductsByAdvancedCondition** (SQL_SELECT)
   - 테이블: PRODUCTS, CATEGORIES, BRANDS (LEFT JOIN)
   - JOIN 관계: 
     - PRODUCTS.category_id = CATEGORIES.category_id
     - PRODUCTS.brand_id = BRANDS.brand_id

4. **selectProductsByCategory** (SQL_SELECT)
   - 테이블: PRODUCTS

5. **updateProductStock** (SQL_UPDATE)
   - 테이블: PRODUCTS

6. **updateProductDynamic** (SQL_UPDATE)
   - 테이블: PRODUCTS

7. **insertProductDynamic** (SQL_INSERT)
   - 테이블: PRODUCTS

8. **deleteProductsByCondition** (SQL_UPDATE)
   - 테이블: PRODUCTS (논리적 삭제)

9. **countProductsByCondition** (SQL_SELECT)
   - 테이블: PRODUCTS

### 3.4 예상 생성 컴포넌트 및 관계
- **SQL_SELECT 컴포넌트**: 약 80개
- **SQL_INSERT 컴포넌트**: 약 8개
- **SQL_UPDATE 컴포넌트**: 약 12개
- **SQL_DELETE 컴포넌트**: 약 5개
- **SQL_MERGE 컴포넌트**: 약 3개 (UPSERT 패턴)
- **SQL_CALL 컴포넌트**: 약 2개 (저장 프로시저)
- **USE_TABLE 관계**: 약 120개
- **JOIN_EXPLICIT 관계**: 약 15개 (LEFT JOIN 구문)
- **JOIN_IMPLICIT 관계**: 약 5개 (WHERE 절 조인)

## 4. Java 분석 정보 (4단계)

### 4.1 분석 대상 Java 파일
총 **55개** Java 파일

#### 레이어별 구성:
- **Controller 레이어**: 9개 파일
- **Service 레이어**: 10개 파일  
- **DAO 레이어**: 7개 파일
- **Model 레이어**: 2개 파일
- **기타**: 27개 파일 (유틸리티, 테스트, 에러 처리 등)

### 4.2 주요 Java 클래스 분석

#### UserController.java
**클래스 정보**:
- 패키지: com.example.controller
- 클래스명: UserController
- 타입: class
- 어노테이션: @Controller, @RequestMapping("/user")

**메서드 정보 (주요 비즈니스 메서드)**:
1. **getUserList** (GET /user/list)
   - 파라미터: name, email, status, page, size
   - 호출: userService.getUsersByCondition()

2. **searchUsers** (POST /user/search)
   - 파라미터: searchParams, page, size
   - 호출: userService.getUsersByAdvancedCondition()

3. **getUsersByType** (GET /user/dynamic/{type})
   - 파라미터: type, page, size
   - 호출: userService.getUsersByType()

**비즈니스 로직 메서드**:
- validateSearchParameters()
- validateAdvancedSearchParameters()
- calculateUserStatistics()
- analyzeSearchResults()
- calculateTypeStatistics()
- applyPaging()

#### UserService.java
**클래스 정보**:
- 패키지: com.example.service
- 클래스명: UserService
- 타입: class

**메서드 정보**:
1. **getUsersByCondition**
   - 호출: userDao.findUsersByCondition()

2. **getUsersByAdvancedCondition**
   - 호출: userDao.findUsersByAdvancedCondition()

3. **getUsersByType**
   - 호출: userDao.findUsersByType()

4. **createUser**
   - 호출: userDao.createUser()

5. **getUserStatistics**
   - 호출: userDao.getTotalUserCount(), userDao.getActiveUserCount() 등

#### UserDao.java
**클래스 정보**:
- 패키지: com.example.dao
- 클래스명: UserDao
- 타입: class

**메서드 정보 (MyBatis + Direct SQL)**:
1. **findUsersByCondition**
   - MyBatis 호출: com.example.mapper.UserMapper.findUsersByCondition
   - Direct SQL: 복잡한 동적 조건 처리

2. **findUsersByAdvancedCondition**
   - MyBatis 호출: com.example.mapper.UserMapper.findUsersByAdvancedCondition

3. **findUsersByType**
   - MyBatis 호출: com.example.mapper.UserMapper.findUsersByType

4. **findUserById**
   - MyBatis 호출: com.example.mapper.UserMapper.findUserById

5. **createUser**
   - MyBatis 호출: com.example.mapper.UserMapper.createUser

**직접 쿼리 메서드**:
- **findUsersByDirectQuery**: StringBuilder를 사용한 동적 SQL 생성
  ```java
  StringBuilder query = new StringBuilder();
  query.append("SELECT u.user_id, u.username, u.full_name, u.email, u.phone, ");
  query.append("u.status, u.user_type, u.created_date, u.last_login_date ");
  query.append("FROM users u WHERE 1=1 ");
  ```

#### User.java (Model)
**클래스 정보**:
- 패키지: com.example.model
- 클래스명: User
- 타입: class

**속성 정보**:
- id (Long)
- username (String)
- email (String)
- password (String)
- name (String)
- age (Integer)
- status (String)
- userType (String)
- createdDate (Date)
- updatedDate (Date)
- phone (String)
- address (String)

**비즈니스 메서드**:
- isActive(), isAdmin(), isPremium(), isAdult()
- getDisplayName(), getMaskedEmail(), getMaskedPhone()
- getAccountAgeInDays(), getStatusDisplayName()
- canAccessAdminFeatures(), canAccessPremiumFeatures()

### 4.3 예상 생성 컴포넌트 및 관계

#### 클래스 컴포넌트:
- **총 클래스 수**: 약 55개
- **Controller 클래스**: 9개
- **Service 클래스**: 10개
- **DAO 클래스**: 7개
- **Model 클래스**: 2개
- **기타 클래스**: 27개

#### 메서드 컴포넌트:
- **총 메서드 수**: 약 300개 (비즈니스 로직 메서드만)
- **Controller 메서드**: 약 30개
- **Service 메서드**: 약 80개
- **DAO 메서드**: 약 50개
- **Model 메서드**: 약 40개
- **기타 메서드**: 약 100개

#### 관계 정보:
- **INHERITANCE 관계**: 약 5개 (상속 관계)
- **CALL_METHOD 관계**: 약 200개 (메서드 간 호출)
- **CALL_QUERY 관계**: 약 50개 (메서드 → SQL 쿼리)
- **USE_TABLE 관계**: 약 30개 (직접 SQL에서 테이블 사용)

## 5. JSP/JSX 분석 정보 (6단계)

### 5.1 분석 대상 JSP 파일
총 **12개** JSP 파일

### 5.2 분석 대상 JSX 파일 (새로 추가)
총 **5개** JSX 파일

#### 주요 JSP 파일:
1. **user/list.jsp**: 사용자 목록 페이지
2. **user/searchResult.jsp**: 검색 결과 페이지
3. **user/typeList.jsp**: 타입별 사용자 목록
4. **user/error.jsp**: 에러 페이지
5. **product/list.jsp**: 상품 목록 페이지
6. **product/searchResult.jsp**: 상품 검색 결과
7. **기타**: 6개 JSP 파일

#### 주요 JSX 파일 (새로 추가):
1. **UserSearchDashboard.jsx**: 사용자 검색 대시보드 (UserController API 연동)
2. **ProductManagement.jsx**: 상품 관리 시스템 (ProductController API 연동)  
3. **MicroserviceDashboard.jsx**: 마이크로서비스 통합 대시보드 (MicroserviceController API 연동)
4. **ProxyServiceManagement.jsx**: 프록시 서비스 관리 (기존)
5. **VersionedUserManagement.jsx**: 버전 관리 사용자 컴포넌트 (기존)

### 5.3 UserSearchDashboard.jsx 상세 분석 (새로 추가)

#### API 호출 패턴:
- **기본 검색**: GET /user/list (UserController.getUserList)
- **고급 검색**: POST /user/search (UserController.searchUsers)  
- **타입별 검색**: GET /user/dynamic/{type} (UserController.getUsersByType)

#### 프론트엔드 기능:
- React Hooks 기반 상태 관리
- 실시간 검색 및 필터링
- 페이징 처리
- 통계 정보 표시
- 에러 핸들링 및 로딩 상태 관리

#### 예상 생성 컴포넌트:
- **JSX 타입 컴포넌트**: 1개 (UserSearchDashboard.jsx)
- **FRONTEND_API 타입 컴포넌트**: 3개 (각 API 호출당 1개)

### 5.4 ProductManagement.jsx 상세 분석 (새로 추가)

#### API 호출 패턴:
- **상품 목록**: GET /product/list (ProductController.getProductList)
- **고급 검색**: POST /product/search (ProductController.searchProducts)
- **카테고리별 조회**: GET /product/category/{categoryId} (ProductController.getProductsByCategory)
- **재고 업데이트**: POST /product/updateStock (ProductController.updateProductStock)

#### 프론트엔드 기능:
- 상품 검색 및 필터링
- 카테고리별 상품 조회
- 재고 관리 기능
- 상품 통계 표시

#### 예상 생성 컴포넌트:
- **JSX 타입 컴포넌트**: 1개 (ProductManagement.jsx)
- **FRONTEND_API 타입 컴포넌트**: 4개 (각 API 호출당 1개)

### 5.5 MicroserviceDashboard.jsx 상세 분석 (새로 추가)

#### API 호출 패턴:
- **사용자 프로필**: GET /api/user-profile (MicroserviceController.getUserProfile)
- **주문 상세**: GET /api/order-details (MicroserviceController.getOrderDetails)
- **대시보드 데이터**: GET /api/dashboard (MicroserviceController.getDashboardData)
- **통합 검색**: GET /api/search (MicroserviceController.globalSearch)
- **알림 발송**: POST /api/notify (MicroserviceController.sendNotification)

#### 프론트엔드 기능:
- 마이크로서비스 통합 인터페이스
- 실시간 대시보드
- 통합 검색 기능
- 알림 시스템

#### 예상 생성 컴포넌트:
- **JSX 타입 컴포넌트**: 1개 (MicroserviceDashboard.jsx)
- **FRONTEND_API 타입 컴포넌트**: 5개 (각 API 호출당 1개)

### 5.7 INFERRED 컴포넌트 분석 (새로 추가)

#### INFERRED_METHOD (45개):
- **인터페이스 구현체 추론**: UserMapper → UserMapperImpl 등
- **상속 관계 추론**: BaseService → ConcreteService 오버라이드 메서드
- **동적 프록시 추론**: Spring AOP, MyBatis 프록시 메서드
- **패턴 기반 추론**: CRUD 패턴에서 누락된 메서드들

#### INFERRED_QUERY (25개):
- **동적 쿼리 패턴**: UserDao의 동적 WHERE 조건 생성
- **템플릿 쿼리**: 반복 패턴에서 추론된 쿼리들
- **배치 쿼리**: 대량 처리 패턴에서 추론
- **캐시 쿼리**: 성능 최적화 패턴에서 추론

#### INFERRED_API_URL (15개):
- **Ajax 패턴**: JavaScript에서 동적 URL 생성
- **Form Action**: JSP form의 동적 action 속성
- **REST 패턴**: RESTful 규칙에 따른 누락 엔드포인트 추론
- **버전 관리**: API 버전별 엔드포인트 추론

#### INFERRED_TABLE (8개):
- **로그 테이블**: 시스템 패턴에서 추론 (AUDIT_LOGS, ERROR_LOGS)
- **세션 테이블**: 사용자 관리 패턴에서 추론 (USER_SESSIONS)
- **캐시 테이블**: 성능 패턴에서 추론 (CACHE_DATA)
- **배치 테이블**: 작업 관리 패턴에서 추론 (BATCH_JOBS)

#### INFERRED_COLUMN (35개):
- **SELECT * 패턴**: 전체 컬럼 선택에서 개별 컬럼 추론
- **JOIN 패턴**: 조인 관계에서 연결 컬럼 추론
- **인덱스 패턴**: 성능 최적화를 위한 컬럼 추론
- **외래키 패턴**: 관계형 무결성을 위한 컬럼 추론

#### 예상 생성 컴포넌트:
- **INFERRED_METHOD 타입**: 45개
- **INFERRED_QUERY 타입**: 25개  
- **INFERRED_API_URL 타입**: 15개
- **INFERRED_TABLE 타입**: 8개
- **INFERRED_COLUMN 타입**: 35개
- **관련 관계**: INFERRED_CALL(35개), INFERRED_USE(25개)

### 5.8 user/list.jsp 상세 분석

#### API 호출 패턴:
- **검색 폼**: GET /user/list
- **고급 검색**: /user/advanced-search

#### 프론트엔드 기능:
- 사용자 검색 (이름, 이메일, 상태별)
- 페이징 처리
- 통계 정보 표시
- 클라이언트 사이드 검증

#### 표시 데이터:
- 사용자 목록 테이블
- 검색 조건 폼
- 통계 정보 패널
- 페이징 네비게이션

### 5.3 예상 생성 컴포넌트 및 관계

#### JSP 컴포넌트:
- **JSP 타입 컴포넌트**: 12개

#### API_URL 컴포넌트:
- **/user/list:GET**: 사용자 목록 조회
- **/user/search:POST**: 사용자 검색
- **/user/dynamic/{type}:GET**: 타입별 사용자 조회
- **/product/list:GET**: 상품 목록 조회
- **기타 API**: 약 10개

#### 관계 정보:
- **JSP → API_URL 관계**: 약 20개
- **JSX → API_URL 관계**: 약 12개 (새로 추가)
- **API_URL → METHOD 관계**: 약 32개 (20개 + 12개)
- **INFERRED_API_URL 관계**: 약 15개 (추론된 API 호출)
- **INFERRED_METHOD 관계**: 약 45개 (추론된 메서드 호출)
- **INFERRED_QUERY 관계**: 약 25개 (추론된 쿼리 실행)

## 6. 예상 메타데이터 통계 (JSX 파일 + INFERRED 컴포넌트 반영)

### 6.1 테이블별 예상 레코드 수

#### projects 테이블:
- **레코드 수**: 1개
- **project_name**: "sampleSrc"
- **project_path**: "D:\Analyzer\CreateMetaDb\projects\sampleSrc"
- **hash_value**: "-"
- **total_files**: 97

#### files 테이블:
- **레코드 수**: 97개
- **Java 파일**: 55개
- **XML 파일**: 16개
- **JSP 파일**: 12개
- **CSV/SQL 파일**: 4개
- **기타 파일**: 10개

#### classes 테이블:
- **레코드 수**: 55개 (Java 클래스)

#### components 테이블:
- **TABLE 타입**: 17개
- **COLUMN 타입**: 92개
- **CLASS 타입**: 55개
- **METHOD 타입**: 약 300개
- **SQL_SELECT 타입**: 약 80개
- **SQL_INSERT 타입**: 약 10개
- **SQL_UPDATE 타입**: 약 15개
- **SQL_DELETE 타입**: 약 5개
- **JSP 타입**: 12개
- **API_URL 타입**: 약 20개
- **총 예상**: 약 606개

#### tables 테이블:
- **레코드 수**: 17개

#### columns 테이블:
- **레코드 수**: 92개

#### relationships 테이블:
- **INHERITANCE**: 약 5개
- **CALL_METHOD**: 약 200개
- **CALL_QUERY**: 약 50개
- **USE_TABLE**: 약 150개
- **JOIN_EXPLICIT**: 약 15개
- **JOIN_IMPLICIT**: 약 5개
- **총 예상**: 약 425개

### 6.2 전체 메타데이터 요약
- **총 테이블**: 7개 (projects, files, classes, components, tables, columns, relationships)
- **총 예상 레코드**: 약 1,200개
- **가장 큰 테이블**: components (606개), relationships (425개)

## 7. 아키텍처 패턴 분석

### 7.1 레이어드 아키텍처
```
JSP (Presentation) 
  ↓ API 호출
Controller (API Entry)
  ↓ 비즈니스 로직 호출
Service (Business Logic)
  ↓ 데이터 접근 호출
DAO (Data Access)
  ↓ SQL 실행
MyBatis Mapper (Data Mapping)
  ↓ 쿼리 실행
Database (Data Storage)
```

### 7.2 주요 호출 체인 예시

#### 사용자 목록 조회:
1. **user/list.jsp** → GET /user/list
2. **UserController.getUserList()** → userService.getUsersByCondition()
3. **UserService.getUsersByCondition()** → userDao.findUsersByCondition()
4. **UserDao.findUsersByCondition()** → UserMapper.findUsersByCondition
5. **UserMapper.xml** → SELECT 쿼리 실행 → USERS 테이블

#### 사용자 검색:
1. **user/list.jsp** → POST /user/search
2. **UserController.searchUsers()** → userService.getUsersByAdvancedCondition()
3. **UserService.getUsersByAdvancedCondition()** → userDao.findUsersByAdvancedCondition()
4. **UserDao.findUsersByAdvancedCondition()** → UserMapper.findUsersByAdvancedCondition
5. **UserMapper.xml** → 복잡한 동적 SELECT 쿼리 → USERS 테이블

### 7.3 데이터 플로우
- **입력**: JSP 폼 데이터 → Controller 파라미터
- **처리**: Service 비즈니스 로직 → DAO 데이터 접근
- **쿼리**: MyBatis 동적 SQL → 데이터베이스
- **출력**: 결과 데이터 → JSP 화면 렌더링

## 8. 에러 및 문제점 분석 (has_error 정보)

### 8.1 에러가 있는 파일 분석

#### 실제 확인된 에러 파일들 (11개)
1. **SyntaxErrorClass.java**: 구문 오류 (중괄호, 세미콜론 누락)
2. **SyntaxErrorController.java**: 정상 파일 (에러 수정됨)
3. **BrokenMapper.java**: 정상 파일 (에러 수정됨)
4. **MixedErrorMapper.xml**: 부분적 에러 (존재하지 않는 테이블/컬럼 참조)
5. **ErrorController.java**: 에러 처리용 컨트롤러
6. **MixedErrorController.java**: 혼합 에러 컨트롤러
7. **LogicErrorService.java**: 로직 에러 서비스
8. **MixedErrorService.java**: 혼합 에러 서비스
9. **syntaxError.jsp**: 구문 에러 JSP
10. **partialError.jsp**: 부분 에러 JSP
11. **user/error.jsp**: 사용자 에러 JSP

#### 메타디비 에러 처리 현황
- **컴포넌트 에러**: 0개 (has_error='Y' 없음)
- **관계 에러**: 0개 (has_error='Y' 없음)
- **실제 에러 파일**: 11개 식별됨

**⚠️ 문제점**: 실제 에러가 있는 파일들이 메타디비에서 에러로 표시되지 않음

### 8.2 MixedErrorMapper.xml 상세 에러 분석

#### 존재하지 않는 테이블 참조
```sql
-- selectFromNonExistentTable: NONEXISTENT_TABLE 테이블 참조
-- selectComplexError: NONEXISTENT_TABLE 조인 시도
-- update: user_profiles 테이블 참조 (존재하지 않을 수 있음)
-- getUserWithDetails: user_preferences 테이블 참조 (존재하지 않을 수 있음)
```

#### 존재하지 않는 컬럼 참조
```sql
-- selectByName: active_flag 컬럼 참조 (존재하지 않을 수 있음)
-- searchUsers: dept_name 컬럼 참조 (존재하지 않을 수 있음)
-- selectWithNonExistentColumn: NONEXISTENT_COLUMN 컬럼 참조
-- selectComplexError: NONEXISTENT_COLUMN WHERE 조건
```

**⚠️ 문제점**: 이러한 에러들이 메타디비에서 감지되지 않음

### 8.3 에러 처리 개선 필요사항

1. **구문 분석 강화**: Java 파일의 구문 오류 감지
2. **테이블/컬럼 유효성 검사**: XML 쿼리의 테이블/컬럼 존재 여부 확인
3. **에러 플래그 설정**: has_error='Y' 및 error_message 저장
4. **부분 파싱 지원**: 에러가 있어도 정상 부분은 분석 계속

## 9. 특별한 패턴 및 기능

### 9.1 동적 SQL 패턴
- MyBatis `<if>`, `<choose>`, `<foreach>` 태그 사용
- 조건부 WHERE 절 생성
- 동적 ORDER BY 절
- 페이징 처리 (LIMIT/OFFSET)

### 9.2 StringBuilder SQL 패턴
UserDao.java의 findUsersByDirectQuery() 메서드에서 StringBuilder를 사용한 동적 SQL 생성:
```java
StringBuilder query = new StringBuilder();
query.append("SELECT u.user_id, u.username, u.full_name, u.email, u.phone, ");
query.append("u.status, u.user_type, u.created_date, u.last_login_date ");
query.append("FROM users u WHERE 1=1 ");
```

### 8.3 JOIN 관계 패턴
- **명시적 JOIN**: LEFT JOIN orders o ON u.user_id = o.user_id
- **암시적 JOIN**: FROM users u, orders o WHERE u.user_id = o.user_id

### 8.4 비즈니스 로직 패턴
- 데이터 검증 (validateSearchParameters)
- 데이터 가공 (processUserData)
- 통계 계산 (calculateUserStatistics)
- 마스킹 처리 (getMaskedEmail, getMaskedPhone)

## 9. 검증 포인트

메타데이터 생성 후 다음 항목들을 검증해야 합니다:

### 9.1 기본 데이터 정확성
- [ ] 프로젝트 정보가 올바르게 저장되었는가?
- [ ] 파일 수가 97개로 정확한가?
- [ ] 테이블 수가 17개로 정확한가?
- [ ] 컬럼 수가 92개로 정확한가?

### 9.2 컴포넌트 생성 정확성
- [ ] SQL 쿼리 컴포넌트가 올바른 타입으로 생성되었는가?
- [ ] Java 클래스와 메서드가 올바르게 추출되었는가?
- [ ] JSP 파일이 컴포넌트로 등록되었는가?
- [ ] API_URL 컴포넌트가 생성되었는가?

### 9.3 관계 분석 정확성
- [ ] JOIN 관계가 올바르게 분석되었는가?
- [ ] 메서드 호출 관계가 정확한가?
- [ ] 쿼리 호출 관계가 올바른가?
- [ ] 테이블 사용 관계가 정확한가?

### 9.4 레이어 분류 정확성
- [ ] Controller 클래스가 CONTROLLER 레이어로 분류되었는가?
- [ ] Service 클래스가 SERVICE 레이어로 분류되었는가?
- [ ] DAO 클래스가 REPOSITORY 레이어로 분류되었는가?
- [ ] Model 클래스가 MODEL 레이어로 분류되었는가?

## 10. 결론

sampleSrc 프로젝트는 전형적인 3-tier 웹 애플리케이션 구조를 가지고 있으며, 다음과 같은 특징을 보입니다:

1. **완전한 CRUD 패턴**: 사용자와 상품 관리를 위한 생성, 조회, 수정, 삭제 기능
2. **동적 SQL 활용**: MyBatis를 통한 복잡한 조건부 쿼리
3. **레이어드 아키텍처**: Controller-Service-DAO 패턴의 명확한 분리
4. **비즈니스 로직**: 데이터 검증, 가공, 통계 계산 등의 풍부한 비즈니스 로직
5. **JOIN 관계**: 명시적/암시적 JOIN을 통한 테이블 간 관계 활용

이 정답지를 바탕으로 메타데이터 생성 로직의 정확성을 검증하고, 누락되거나 잘못 분석된 부분을 식별할 수 있을 것입니다.

---

**작성자**: SourceAnalyzer  
**작성일**: 2025-09-19  
**문서 버전**: 1.0  
**검토 상태**: 초안 완료
