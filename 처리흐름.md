
모든 경로는 유닉스 스타일 /로 변환하여 처리한다.


main.py

파라메터 세팅
    --project-name {project-name}
    --clear-db

DB생성
    {project-name}/metadata.db가 없으면 생성
    {project-name}/sqlcontent.db가 없으면 생성
    
프로젝트 생성
    projects에 현재 {project-name}이 없으면 생성한다.
    존재하는 경우 이후 project_id는 등록된 프로젝트의 건으로 처리한다.
    
file 등록
    path : 파일명을 제외한 {project-name}/src를 루트로한 상대경로로 저장한다.
    files insert

테이블정보 로딩
    ALL_TABLES.csv, ALL_COLUMNS.csv 파일을 업로드 해서 tables, columns, components, relationships를 등록한다.

[개별부] XML 로딩
    쿼리만 추출함 => sqlText
    sqlid
[개별부] JAVA 로딩
    classess, file_id, method component, sqlid
    쿼리만 추출함 => sqlText
[개별부] JPA 로딩
    classess, file_id, method component, sqlid
    쿼리만 추출함 => sqlText
    
    method -> xml쿼리 relationships는 어떻게?
    method -> method 호출 (controller -> service)

[공통부] sqlcontent.db 저장 및 components 등록
    sqlText insert
        sqlid, file_id -> component, sqlcontent    
    
[공통부] Table추출
    sqlText에서 Table만 추출
    tables에 미존재시 Inferred Table Insert => tables, components
    
[공통부] Join 관계 추출
    Join관계 추출
    columns에 미존재시 Inferred Column Insert => columns, components, relationsips
    
BackEnd API URL 추출
    Java파일에서 BackEnd API URL 추출 -> components, relationships (API_URL=>Method)
        
FrontEnd API URL 추출        
    JSP, JSX... 파일에서 FrontEnd API URL 추출 
        -> API_URL이 존재하면 components.file_id를 Fronend File_id로 Update, relationships은 이미 전단계에서 Method와 연결되어 있음.
        -> API_URL이 존재하지 않으면 components.file_id를 Fronend File_id로 Insert, relationships 등록불가 (끊어진 Front)


---

