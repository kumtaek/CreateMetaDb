# 정답지 최종 (sampleSrc 직접 분석 결과)
업데이트: 2025-10-01 18:50:54

## parent_id 설정 규칙

| 컴포넌트 타입 | parent_id 값 | 설명 |
|---------------|---------------|------|
| **METHOD** | classes.class_id | 메서드가 속한 클래스의 ID |
| **COLUMN** | TABLE의 component_id | 컬럼이 속한 테이블의 컴포넌트 ID |
| **API_URL** | None | 독립적 API 엔드포인트 |
| **SQL_*** | None | 독립적 SQL 쿼리 |
| **TABLE** | None | 독립적 데이터베이스 테이블 |
| **FRONTEND_API** | None | 독립적 프론트엔드 API 호출 |

## 1. 기본 수치 정답지

### 파일 수치
- **files**: 141개 (97개 Java + 8개 XML + 13개 JSP + 7개 JSX + 2개 TSX + 5개 Vue + 기타 9개)

### Components 수치
- **METHOD**: 1216개 (Controller 메서드)
- **API_URL**: 219개 (Controller 엔드포인트)
- **SQL_SELECT**: 119개 (MyBatis XML)
- **SQL_INSERT**: 24개 (MyBatis XML)
- **SQL_UPDATE**: 30개 (MyBatis XML)
- **SQL_DELETE**: 19개 (MyBatis XML)
- **SQL_MERGE**: 18개 (MyBatis XML + Java 파일)
- **JSP**: 13개 (프론트엔드)
- **JSX**: 26개 (프론트엔드)
- **TSX**: 0개 (프론트엔드)
- **VUE**: 5개 (프론트엔드)
- **API_URL**: 219개 (백엔드 + 프론트엔드 통합)

### Relationships 수치
- **CALL_METHOD**: 113개 (API_URL → METHOD)
- **USE_TABLE**: 35개 (SQL → TABLE)
- **CALL_QUERY**: 53개 (METHOD → SQL)
- **USE_COLUMN**: 5개 (SQL → COLUMN, 추정)
- **JOIN_EXPLICIT**: 69개 (SQL JOIN 관계)
- **JOIN_IMPLICIT**: 16개 (SQL JOIN 관계)
- **JOIN_MERGE**: 0개 (SQL JOIN 관계)

### Tables 수치
- **총 TABLE**: 23개
- **테이블 목록**: AUDIT_LOG, CATEGORIES, CUSTOMERS, DEPARTMENTS, DISCOUNTS, FILE_UPLOADS, NOTIFICATIONS, ORDERS, ORDER_ITEMS, PAYMENT_METHODS, PRODUCTS, RELATED_DATA, REVIEWS, ROLES, SHIPPING_INFO, SHOPPING_CART, SYSTEM_CONFIG, USERS, USER_PROFILES, USER_ROLES, USER_SESSIONS, USER_TYPES, WISHLIST

### Columns 수치
- **총 COLUMN**: 56개

## 2. 정답지 최종 수치 요약

| 항목 | 정답지 수치 |
|------|-------------|
| **files** | 141개 |
| **METHOD** | 1216개 |
| **API_URL** | 219개 |
| **SQL_SELECT** | 119개 |
| **SQL_INSERT** | 24개 |
| **SQL_UPDATE** | 30개 |
| **SQL_DELETE** | 19개 |
| **SQL_MERGE** | 18개 |
| **총 SQL 쿼리** | 210개 |
| **JSP** | 13개 |
| **JSX** | 26개 |
| **TSX** | 0개 |
| **VUE** | 5개 |
| **API_URL** | 219개 |
| **TABLE** | 23개 |
| **COLUMN** | 56개 |
| **CALL_METHOD** | 113개 |
| **USE_TABLE** | 35개 |
| **CALL_QUERY** | 53개 |
| **USE_COLUMN** | 5개 |
| **JOIN_EXPLICIT** | 69개 |
| **JOIN_IMPLICIT** | 16개 |
| **JOIN_MERGE** | 0개 |

**참고**: 이 수치는 sampleSrc 소스코드를 직접 분석한 결과이며, 메타디비와 무관하게 독립적으로 작성됨.
업데이트 시각: 2025-10-01 18:50:54
