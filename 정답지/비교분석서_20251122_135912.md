# 메타DB vs 정답지 비교분석서

**분석일시**: 2025-11-22 13:59:12
**대상 프로젝트**: sampleSrc
**정답지 파일**: 정답지_백엔드_매핑표.md

---

## 1. 종합 요약

| 구분 | 정답지 | 메타DB | 일치율 | 평가 |
|------|--------|--------|--------|------|
| MyBatis XML | 41개 | 51개 | - | 정답지 불완전 |
| JPA Native SQL | 20개 | 0개 | 0% | 메타DB 미인식 |
| Java 문자열 SQL | 11개 | 30개 | - | 정답지 불완전 |

### 핵심 발견사항
1. **MyBatis XML**: 메타DB가 실제 XML 파일의 모든 쿼리(51개)를 정확히 인식함. 정답지가 불완전함.
2. **JPA Native SQL**: 메타DB가 @Query(nativeQuery=true) 어노테이션을 인식하지 못함. 심각한 누락.
3. **Java 문자열 SQL**: 메타DB가 정답지보다 더 많은 SQL을 인식 (테스트 코드 포함).

---

## 2. MyBatis XML 상세 비교

### 2.1 실제 XML 파일 현황 (51개)

| 파일명 | 쿼리 수 | 쿼리 ID |
|--------|---------|---------|
| UserMapper.xml | 21개 | createUser, deleteUser, existsByEmail, existsByUsername, findUserById, findUsersByAdvancedCondition, findUsersByComplexSearch, findUsersByCondition, findUsersByType, findUsersWithOrderInfo, getActiveUserCount, getMonthlyRegistrationStats, getNewUsersTodayCount, getPremiumUserCount, getTotalUserCount, getUserStatisticsByStatus, getUserStatisticsByType, isEmailVerified, isPhoneVerified, updateLastLogin, updateUser |
| ProductMapper.xml | 14개 | analyzeProductSalesPerformance, countProductsByCondition, deleteProduct, deleteProductsByCondition, getProductStatsByCategory, insertProductDynamic, selectLowStockProducts, selectProductById, selectProductsByAdvancedCondition, selectProductsByBrand, selectProductsByCategory, selectProductsByCondition, updateProductDynamic, updateProductStock |
| UserManagementMapper.xml | 6개 | deleteInactiveUsers, insertUser, selectUserById, selectUserStatistics, selectUsers, updateUserStatus |
| ImplicitJoinMapper.xml | 5개 | selectUsersWithActiveOrders, selectUsersWithOrderDetails, selectUsersWithOrderStatistics, selectUsersWithOrders, selectUsersWithRecentOrders |
| MergeQueryMapper.xml | 5개 | mergeCategoryStatistics, mergeOrderSummary, mergeProductInventory, mergeUserActivityLog, mergeUserStatistics |

### 2.2 정답지 오류 목록 (20개)

정답지에 나열되었으나 실제 XML 파일에 존재하지 않는 쿼리 ID:

| No | 정답지 기재 쿼리 ID | 상태 |
|----|---------------------|------|
| 1 | findUsersByActivity | 존재하지 않음 |
| 2 | findUsersByAgeRange | 존재하지 않음 |
| 3 | findUsersByCreatedDate | 존재하지 않음 |
| 4 | findUsersByEmail | 존재하지 않음 |
| 5 | findUsersByFullName | 존재하지 않음 |
| 6 | findUsersByLastLoginDate | 존재하지 않음 |
| 7 | findUsersByLocation | 존재하지 않음 |
| 8 | findUsersByPreferences | 존재하지 않음 |
| 9 | findUsersByStatus | 존재하지 않음 |
| 10 | findUsersByUserType | 존재하지 않음 |
| 11 | findUsersByUsername | 존재하지 않음 |
| 12 | insertProduct | 존재하지 않음 |
| 13 | selectOrdersWithUsers | 존재하지 않음 |
| 14 | selectProductsByCreatedDate | 존재하지 않음 |
| 15 | selectProductsByPriceRange | 존재하지 않음 |
| 16 | selectProductsByStatus | 존재하지 않음 |
| 17 | selectProductsByStockLevel | 존재하지 않음 |
| 18 | selectProductsWithCategories | 존재하지 않음 |
| 19 | softDeleteUser | 존재하지 않음 |
| 20 | updateUserLastLogin | 존재하지 않음 |

### 2.3 정답지 누락 목록 (31개)

실제 XML 파일에 존재하나 정답지에 없는 쿼리 ID:

- analyzeProductSalesPerformance, countProductsByCondition, deleteInactiveUsers
- deleteProductsByCondition, deleteUser, existsByEmail, existsByUsername
- getActiveUserCount, getMonthlyRegistrationStats, getNewUsersTodayCount
- getPremiumUserCount, getProductStatsByCategory, getTotalUserCount
- getUserStatisticsByStatus, getUserStatisticsByType, insertProductDynamic
- insertUser, isEmailVerified, isPhoneVerified, mergeCategoryStatistics
- mergeOrderSummary, mergeUserActivityLog, selectProductById
- selectUserById, selectUserStatistics, selectUsers
- selectUsersWithActiveOrders, selectUsersWithOrderDetails
- selectUsersWithOrderStatistics, selectUsersWithRecentOrders, updateLastLogin

### 2.4 MyBatis XML 평가

| 항목 | 결과 |
|------|------|
| 메타DB 인식률 | 100% (51/51) |
| 정답지 정확도 | 약 49% (정답지 41개 중 20개가 잘못됨) |
| 결론 | **메타DB 정상**, 정답지 수정 필요 |

---

## 3. JPA Native SQL 상세 비교

### 3.1 정답지 기재 내역 (20개)

정답지에 기재된 JPA Repository SQL:

**JoinConditionRepository.java (9개)**
1. insertUserProfileWithUserType (INSERT)
2. updateOrderStatusWithCustomer (UPDATE)
3. deleteInactiveUsersWithRoles (DELETE)
4. insertProductWithCategory (INSERT)
5. updateUserWithDepartment (UPDATE)
6. deleteCustomerWithOrders (DELETE)
7. insertOrderItemWithProduct (INSERT)
8. updateUserWithProfile (UPDATE)
9. deleteProductWithCategory (DELETE)

**MergeQueryRepository.java (5개)**
10. mergeUserStatisticsWithJpa (MERGE)
11. mergeProductInventoryWithJpa (MERGE)
12. mergeOrderSummaryWithJpa (MERGE)
13. mergeUserActivityLogWithJpa (MERGE)
14. mergeCategoryStatisticsWithJpa (MERGE)

**UserRepository.java (2개)**
15. findByUserTypeAndCreatedAtAfterNative (SELECT)
16. countUsersByStatusNative (SELECT)

**OrderRepository.java (2개)**
17. findByStatusAndAmountNative (SELECT)
18. findOrdersWithUserInDateRangeNative (SELECT)

**ProductRepository.java (2개)**
19. findByNameAndPriceNative (SELECT)
20. findByStatusWithCategoryNameNative (SELECT)

### 3.2 메타DB 인식 현황

| 항목 | 결과 |
|------|------|
| 인식된 JPA SQL | 0개 |
| 누락된 JPA SQL | 20개 (100% 누락) |

### 3.3 실제 파일 확인 결과

JPA Repository 파일들은 메타DB files 테이블에 등록됨:
- CategoryRepository.java
- JoinConditionRepository.java
- MergeQueryRepository.java
- OrderRepository.java
- ProductRepository.java
- UserRepository.java

그러나 해당 파일들의 @Query(nativeQuery=true) 어노테이션 SQL이 components 테이블에 추출되지 않음.

### 3.4 JPA Native SQL 평가

| 항목 | 결과 |
|------|------|
| 메타DB 인식률 | 0% (0/20) |
| 원인 분석 | java_loading.py가 @Query 어노테이션 파싱 미지원 |
| 결론 | **메타DB 개선 필요** - JPA @Query 파싱 기능 추가 필요 |

---

## 4. Java 문자열 SQL 상세 비교

### 4.1 정답지 기재 내역 (11개)

**MergeQueryDao.java (5개)**
1. mergeUserStatistics (MERGE)
2. mergeProductInventory (MERGE)
3. mergeOrderSummary (MERGE)
4. mergeUserActivityLog (MERGE)
5. mergeCategoryStatistics (MERGE)

**JoinQueryDao.java (6개)**
6. insertUserOrderStatistics (INSERT)
7. updateUserStatusBasedOnOrders (UPDATE)
8. deleteInactiveUsersWithNoActivity (DELETE)
9. insertProductRecommendations (INSERT)
10. updateProductPricesBasedOnAnalysis (UPDATE)
11. deleteOldOrderItems (DELETE)

### 4.2 메타DB 인식 현황 (30개 메서드)

메타DB가 인식한 Java DAO SQL 메서드:

| 파일 | 메서드 | SQL 타입 |
|------|--------|----------|
| MergeQueryDao | mergeUserStatistics | MERGE, SELECT, UPDATE, INSERT |
| MergeQueryDao | mergeProductInventory | MERGE, SELECT, UPDATE, INSERT |
| MergeQueryDao | mergeOrderSummary | MERGE, SELECT, UPDATE, INSERT |
| MergeQueryDao | mergeUserActivityLog | MERGE, SELECT, UPDATE, INSERT |
| MergeQueryDao | mergeCategoryStatistics | MERGE, SELECT, UPDATE, INSERT |
| JoinQueryDao | insertUserOrderStatistics | INSERT, SELECT |
| JoinQueryDao | updateUserStatusBasedOnOrders | UPDATE, SELECT |
| JoinQueryDao | deleteInactiveUsersWithNoActivity | DELETE, SELECT |
| JoinQueryDao | insertProductRecommendations | INSERT, SELECT |
| JoinQueryDao | updateProductPricesBasedOnAnalysis | UPDATE, SELECT |
| JoinQueryDao | deleteOldOrderItems | DELETE, SELECT |
| CoreSqlPatternDao | executeOracleDataWarehouseQuery | SELECT |
| HybridSqlPatternDao | analyzeCustomerOrderPaymentFlow | SELECT |
| HybridSqlPatternDao | analyzeSalesTrends | SELECT |
| HybridSqlPatternDao | buildOrganizationHierarchy | SELECT |
| HybridSqlPatternDao | findUserOrdersWithHybridPattern | SELECT |
| TestComplexJavaPatternDao | (9개 테스트 메서드) | 다양함 |
| TestEnhancedSqlDao | (10개 테스트 메서드) | 다양함 |

### 4.3 Java 문자열 SQL 평가

| 항목 | 결과 |
|------|------|
| 정답지 메서드 인식률 | 100% (11/11) |
| 추가 인식 | 19개 (테스트 코드 + 추가 DAO) |
| 결론 | **메타DB 정상**, 정답지가 테스트 코드를 제외함 |

---

## 5. 결론 및 권장사항

### 5.1 메타DB 평가 결과

| 영역 | 평가 | 상세 |
|------|------|------|
| MyBatis XML | **정상** | 100% 인식 |
| JPA @Query | **미지원** | 0% 인식 - 개발 필요 |
| Java StringBuilder SQL | **정상** | 100% 인식 |

### 5.2 정답지 평가 결과

| 영역 | 평가 | 상세 |
|------|------|------|
| MyBatis XML | **불완전** | 20개 오류, 31개 누락 |
| JPA @Query | **정확** | 실제 파일과 일치 |
| Java StringBuilder SQL | **불완전** | 테스트 코드 미포함 |

### 5.3 권장 조치사항

1. **긴급 (메타DB 개선)**
   - `java_loading.py`에 JPA @Query(nativeQuery=true) 파싱 기능 추가
   - @Modifying 어노테이션과 함께 사용되는 UPDATE/DELETE 쿼리 인식

2. **권장 (정답지 수정)**
   - 정답지_백엔드_매핑표.md 파일을 실제 소스 파일 기준으로 재작성
   - MyBatis XML 섹션 전면 수정 (51개 쿼리로 업데이트)

3. **선택 (메타DB 개선)**
   - JPQL 쿼리 (@Query without nativeQuery) 인식 추가 고려
   - JPA Repository 메서드명 기반 쿼리 추론 기능 고려

---

## 6. 첨부: 상세 데이터

### 6.1 메타DB components 테이블 통계

| component_type | 개수 |
|----------------|------|
| API_URL | 221 |
| METHOD | 1,216 |
| SQL_SELECT | 119 |
| SQL_UPDATE | 30 |
| SQL_INSERT | 24 |
| SQL_DELETE | 19 |
| SQL_MERGE | 18 |
| SQL_QUERY | 19 |
| TABLE | 23 |
| COLUMN | 41 |
| 기타 (JSP, JSX, VUE 등) | 32 |
| **총계** | **1,762** |

---

**분석 완료**
