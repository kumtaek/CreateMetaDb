# sampleSrc 백엔드 매핑 비교분석서 (2025-11-23 10:37:00)

## 검증 범위
- 기대치: `정답지/정답지_백엔드_매핑표.md`
- 실측: 재생성한 `projects/sampleSrc/metadata.db`(파일 스캔→XML→Java 재실행), 보조 확인 `projects/sampleSrc/SqlContent.db`
- 비교 기준: `file_type='INFERRED'`에 묶인 노이즈는 제외하고 실제 소스 경로만 집계

## 요약 결과
- MyBatis: 기대 51 vs 실제 60 (일치 51, 정답지 외 9). `src/com/example/dynamicquery/UserMapper.xml` 추가 쿼리 9건 포함, 누락 없음.
- JPA(@Query/native): 기대 86 vs 실제 96 (일치 86, 정답지 외 10). `src/main/java/com/example/jpa/JoinQueryRepository.java` 9건, `OrderRepository.findOrdersWithInferredBrandMetric` 1건 추가.
- Java 문자열 SQL: 기대 47 vs 실제 153 (일치 47, 정답지 외 106). 추가분은 테스트/성능용 DAO와 서비스 메서드에서 발견.
- 공통 이슈: `files/file_id`에 `inferred/INVENTORY_AUDIT.inferred`가 141건 남아 SQL 컴포넌트 328건을 중복 보유. 현재 비교에서는 제외했으나 DB 청소 필요.

## 상세 비교
### MyBatis XML
- 일치: 정답지 51건 전부 매칭 (`src/main/resources/mybatis/mapper/*.xml` 5개 파일).
- 정답지 외 추가 9건(소스에는 존재): `src/com/example/dynamicquery/UserMapper.xml`의 복합 JOIN/ALIAS 샘플 쿼리 9건 (`findComplexJoins` 등). → 정답지 범위에 포함할지 결정 필요.
- 누락: 없음.

### JPA Native/커스텀 SQL
- 일치: 정답지 86건 모두 탐지(`src/main/java/com/example/jpa/repository/*.java`).
- 정답지 외 추가 10건:
  - `src/main/java/com/example/jpa/JoinQueryRepository.java` 9건(`deleteInactiveUsersWithExists` 등)
  - `src/main/java/com/example/jpa/repository/OrderRepository.java`의 `findOrdersWithInferredBrandMetric_Qry_1`
- 누락: 없음. → 정답지가 최신 소스 반영 필요.

### Java 문자열 기반 SQL
- 일치: 정답지 47건 모두 존재 (`src/main/java/com/example/dao/*QueryDao.java`).
- 정답지 외 추가 106건:
  - 테스트/샘플 DAO: `src/main/java/com/example/testcase/TestEnhancedSqlDao.java` 41건, `TestComplexJavaPatternDao.java` 19건
  - 성능 샘플: `src/main/java/com/example/performance/LargeDataProcessor.java` 34건, `HybridSqlPatternDao.java` 9건
  - 서비스/모델 소수: `src/main/java/com/example/service/UserServiceImpl.java` 2건, `src/main/java/com/example/model/User.java` 1건
- 누락: 없음. → 정답지 스코프 정의(테스트/성능 코드 포함 여부) 후 반영 필요.

## 메타DB 상태 및 원인
- `files` 테이블에 정상 파일 경로가 기록되었으나 `inferred/INVENTORY_AUDIT.inferred` 행이 141건 남아 있고, 이에 연결된 SQL 컴포넌트 328건이 중복으로 존재.
- 원인: 과거 `file_id` 보정 실패 시 fallback으로 inferred 파일을 계속 생성/사용한 이력이 남아 있음. (현재 비교는 `file_type!='INFERRED'`만 사용)
- 영향: 통계/집계 시 쿼리 수가 부풀려지며, API/관계 빌더가 오염될 위험.

## 개선 방안
1. inferred 정리: `files/file_path='inferred'` 및 연결된 컴포넌트/관계 정리 후 재생성(또는 `--clear-metadb` 실행 전에 열린 커넥션 종료). 중복 생성을 막는 guard 추가 검토.
2. MyBatis 추가 쿼리 9건, JPA 추가 10건, Java 문자열 추가 106건에 대해 정답지 반영 여부 결정. 운영 범위에 포함 시 정답지 업데이트.
3. 비교 자동화 시 `file_type='INFERRED'`는 기본 제외 필터로 처리하여 노이즈 차단.
4. 재검증: 위 정리/정답지 보강 후 `python main.py --project-name sampleSrc --clear-metadb` → 파일 스캔이 끝난 뒤 XML/Java 재분석 → 동일 비교 스크립트 재실행.
