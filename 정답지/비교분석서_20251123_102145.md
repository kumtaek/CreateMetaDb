# sampleSrc 백엔드 매핑 비교분석서 (2025-11-23 10:21:45)

## 검증 범위
- 기대치: `정답지/정답지_백엔드_매핑표.md`
- 실측: `projects/sampleSrc/metadata.db` (2025-11-22 생성본, 재생성 미실시), 보조 확인: `projects/sampleSrc/SqlContent.db`

## 주요 결과
- 공통 결함: `files` 테이블 XML 등록 0건, `components`→`files` 조인 시 `('inferred','INVENTORY_AUDIT.inferred')`만 연결됨.
- MyBatis: 기대 51건 vs 실제 19건(일치 7, 누락 44, 정답지 외 12). `src/main/resources/mybatis/mapper/*.xml` 전반이 메타DB에 반영되지 않음.
- JPA: 기대 86건 vs 실제 96건(누락 0, 정답지 외 10). 추가 10건은 실제 소스 존재.
- Java 문자열 SQL: 기대 47건 vs 실제 116건(누락 0, 정답지 외 69). 추가 항목은 테스트/성능용 DAO 소스에 존재.

## 상세 비교 및 원인
- **공통 원인 (메타DB)**  
  - `files`에 XML 0건, `components`의 `file_id`가 모두 `inferred`를 가리켜 소스 경로가 유실됨. (쿼리: `SELECT DISTINCT f.file_path,file_name FROM components c JOIN files f ON c.file_id=f.file_id` → 1건)  
  - `sql_content_manager` 수정 후 재생성 미실시로 개선효과 미반영.

- **MyBatis 누락 44건 (정답지 문제 없음, 메타DB 문제)**  
  - 파일: `src/main/resources/mybatis/mapper/ImplicitJoinMapper.xml`, `MergeQueryMapper.xml`, `ProductMapper.xml`, `UserManagementMapper.xml`, `UserMapper.xml` 등 실제 존재.  
  - `SqlContent.db`에는 쿼리명이 존재하나 `metadata.db`의 `components`/`files`에 맵핑되지 않음 → 스캔/적재 실패로 판단.
  - 정답지 외 추가 12건: `src/com/example/dynamicquery/UserMapper.xml` 계열. 소스에 존재하므로 정답지에는 포함되지 않은 확장 범위.

- **JPA 추가 10건 (정답지 보강 필요)**  
  - `src/main/java/com/example/jpa/JoinQueryRepository.java` 9건(@Query native)  
  - `src/main/java/com/example/jpa/repository/OrderRepository.java`의 `findOrdersWithInferredBrandMetric` 1건  
  - 정답지가 최신 소스 반영을 못한 케이스, 메타DB는 정상 포착.

- **Java 문자열 SQL 추가 69건 (정답지 보강 선택)**  
  - `src/main/java/com/example/performance/HybridSqlPatternDao.java` 9건  
  - `src/main/java/com/example/testcase/TestComplexJavaPatternDao.java` 19건  
  - `src/main/java/com/example/testcase/TestEnhancedSqlDao.java` 41건  
  - 테스트/성능용 DAO라 정답지 범위 결정에 따라 포함 여부 판단 필요. 메타DB는 포착됨.

## 개선 방안
1. 메타DB 재생성: `python main.py --project-name sampleSrc --clear-metadb` (코드 수정 사항 반영).  
2. 재생성 후 검증:  
   - `files`에 `src/main/resources/mybatis/mapper/*.xml` 등 실제 경로가 등록됐는지 확인.  
   - `components.file_id`가 `inferred`가 아닌 실제 경로를 가리키는지 확인.  
3. 정답지 업데이트 여부 결정:  
   - JoinQueryRepository/OrderRepository 추가 10건, 테스트/성능 DAO 69건을 포함할지 정책 확정 후 정답지 갱신.  
4. 재검증: 재생성된 메타DB로 동일 스크립트 실행 후 MyBatis 누락 44건 해소 여부 확인.***
